<form method="post" action="{{ path('examen_orina_quimico_delete', {'id': examen_orina_quimico.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ examen_orina_quimico.id) }}">
    <button class="btn">Delete</button>
</form>
