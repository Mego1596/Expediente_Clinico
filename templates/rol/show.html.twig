{% extends 'base.html.twig' %}

{% block title %}Rol{% endblock %}

{% block body %}
    <h1>Rol</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rol.id }}</td>
            </tr>
            <tr>
                <th>NombreRol</th>
                <td>{{ rol.nombreRol }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ rol.descripcion }}</td>
            </tr>
            <tr>
                <th>Creado_en</th>
                <td>{{ rol.creadoEn ? rol.creadoEn|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Actualizado_en</th>
                <td>{{ rol.actualizadoEn ? rol.actualizadoEn|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rol_index') }}">back to list</a>

    <a href="{{ path('rol_edit', {'id': rol.id}) }}">edit</a>

    {{ include('rol/_delete_form.html.twig') }}
{% endblock %}
