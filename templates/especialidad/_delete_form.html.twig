<form method="post" action="{{ path('especialidad_delete', {'id': especialidad.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ especialidad.id) }}">
    <button class="btn">Delete</button>
</form>
