<form method="post" action="{{ path('tipo_habitacion_delete', {'id': tipo_habitacion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tipo_habitacion.id) }}">
    <button class="btn">Delete</button>
</form>
