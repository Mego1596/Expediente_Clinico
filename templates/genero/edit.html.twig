{% extends 'base.html.twig' %}

{% block title %}Edit Genero{% endblock %}

{% block body %}
    {{ include('genero/_form.html.twig', {'button_label': 'Update'}) }}
    <a class="btn btn-secondary" href="{{ path('genero_index') }}">back to list</a>
{% endblock %}
