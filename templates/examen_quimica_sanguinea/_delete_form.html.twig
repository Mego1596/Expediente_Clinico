<form method="post" action="{{ path('examen_quimica_sanguinea_delete', {'id': examen_quimica_sanguinea.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ examen_quimica_sanguinea.id) }}">
    <button class="btn">Delete</button>
</form>
